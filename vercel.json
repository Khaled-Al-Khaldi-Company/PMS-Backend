{
  "experimentalServices": {
    "pms-erp-backend": {
      "type": "web",
      "routePrefix": "/",
      "buildCommand": "npm install && npx prisma generate && npx prisma db push && npm run build"
    }
  }
}
