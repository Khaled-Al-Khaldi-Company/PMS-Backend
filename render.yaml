services:
  - type: web
    name: pms-erp-backend
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm install && npx prisma generate && npx prisma db push && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        value: postgresql://postgres.eqizkdvjcopgmyjdpjbs:0533925011MfM@aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
      - key: JWT_SECRET
        value: pms_super_secret_key_2026_production
